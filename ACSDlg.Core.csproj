<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- La lib ne compile que son propre code, jamais les samples. -->
  <ItemGroup>
    <Compile Remove="samples/**/*.cs" />
  </ItemGroup>

</Project>
